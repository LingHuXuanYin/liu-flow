@file:Suppress("unused", "ktlint:filename")

// DEPRECATED (2026-08-23):
// The in-app StatusBar composable was removed in favour of Android's
// system status bar. Each screen now relies on `statusBarsPadding()` /
// `systemBarsPadding()` to respect the system insets instead.
// This file is intentionally left empty so the file system state stays
// close to the previous commit; delete it whenever you clean up.
