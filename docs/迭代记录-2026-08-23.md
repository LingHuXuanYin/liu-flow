# 迭代记录 - 2026-08-23

##  版本信息

迭代版本号：V0.5.2

迭代版本标题：V0.5.0 焦点屏分类两行 + 时长 30s / V0.5.1 串行状态机抽出 SessionStateMachine / V0.5.2 Rest 屏绿叶版 + History 屏改版

迭代主要内容：基于 PRD v0.1 与 HTML 原型，完成「流 Flow」Android 客户端首版开发，9 屏全打通，4 套主题，统计 / 热力图 / 数据导出齐备；V0.2.0 补后台保活 + 视觉打磨；V0.3.0 补提示音 / 自定义时长 / 重新开始 / DataStore 备份验证；V0.5.0 焦点屏分类两行 + 时长 30 秒起步；V0.5.1 抽出 SessionStateMachine 状态机独立文件、屏切换 / timer 状态变更全串行；V0.5.2 焦点时长预设改成 15/25/40/60 + 休息屏绿叶动效版 + 记录屏按 prototype 改版（今日 3 KPI + 本周 +X% 趋势 + 7 天 Mon-Sun 柱图 + 最近完成列表）。

---

## 一、已完成功能

| 模块     | 功能点                                                       | 状态   | 备注                                 |
| -------- | ------------------------------------------------------------ | ------ | ------------------------------------ |
| 工程     | Gradle 8.9 + AGP 8.5 + Kotlin 2.0 工程骨架                   | ✅ 完成 | libs.versions.toml 集中管理          |
| 工程     | JDK 17 / compileSdk 34 / minSdk 26 配置                      | ✅ 完成 | -                                    |
| 主题     | 4 套 MD3 配色（经典 / 静夜 / 森林 / 薄暮）                    | ✅ 完成 | 浅深各 8 套色板                      |
| 数据     | Room 2.6 数据库（SessionEntity / DailyGoalEntity）           | ✅ 完成 | KSP 编译                             |
| 数据     | DataStore Preferences 持久化设置                             | ✅ 完成 | -                                    |
| 业务     | 倒计时状态机（IDLE/RUNNING/PAUSED/RESTING/COMPLETED/ABANDONED） | ✅ 完成 | TimerController                      |
| 主屏     | Focus 屏（任务输入 + 6 分类 + 时长 + 点击圆环开始）          | ✅ 完成 | 移除原 FAB                           |
| 专注     | Running 屏（全屏倒计时 + 短按暂停/继续 + 长按 3s 红色扫光放弃） | ✅ 完成 | 自定义 pointerInput                  |
| 休息     | Rest 屏（自动 5 min 倒计时）                                 | ✅ 完成 | -                                    |
| 记录     | History 屏（今日卡 + 7 天柱状图 + 最近列表）                 | ✅ 完成 | -                                    |
| 统计     | Stats 屏（4 KPI + Streak + 周柱 + 分类条 + 目标环 + 工作日/周末 + 最佳记录） | ✅ 完成 | -                                    |
| 统计     | Heatmap 屏（7×24 SVG 网格，alpha 渐变）                      | ✅ 完成 | 168 格                               |
| 统计     | Weekly 屏（7 天分钟柱状图）                                 | ✅ 完成 | -                                    |
| 我的     | Me 屏（数据导出 JSON/CSV + 清空数据 + 关于）                 | ✅ 完成 | -                                    |
| 设置     | Settings 屏（4 主题 + 浅/深 + 默认时长 + 休息时长 + 每日目标 + 声音/震动） | ✅ 完成 | -                                    |
| 导出     | JSON 导出（kotlinx-serialization）                           | ✅ 完成 | 系统 SAF 文件选择器                  |
| 导出     | CSV 导出（opencsv）                                          | ✅ 完成 | 带 UTF-8 BOM，Excel 友好             |
| 统计     | StatsCalculator 纯函数 + JUnit 单元测试                      | ✅ 完成 | overview / streak / heatmap / last7Days |
| 部署     | SETUP.md（从 JDK 17 到首次 Run 全流程）                      | ✅ 完成 | 模拟器/真机/网络问题排错             |
| 文档     | README.md（技术栈 + 目录 + 9 屏说明）                        | ✅ 完成 | -                                    |

---

| 工程     | 后台保活 — ForegroundService + 通知 + 启动/停止自动联动     | ✅ 完成 | 9 屏迁 systemBarsPadding，状态机驱动启停 |
| 全局     | 移除 in-app StatusBar（v0.1.0 旧条目，迁 systemBarsPadding）| ✅ 完成 | -                                    |
| 通知     | 专注/休息/完成 三态通知，importance LOW 不响铃              | ✅ 完成 | 点击回主屏                          |
| 通知     | 运行时申请 POST_NOTIFICATIONS（Android 13+）                | ✅ 完成 | MainActivity 处理                    |
| 统计     | Stats 加 MiniHeatmapCard 入口（取代顶部 Share 图标）        | ✅ 完成 | 内嵌 7×24 mini 预览                  |
| 统计     | DailyGoalCard 打磨：84dp 环 + 「还差 N 次」动态提示        | ✅ 完成 | -                                    |
| 我的     | 导出按钮加副标题（.json / .csv 描述）                        | ✅ 完成 | 格式说明更清楚                      |

---

| 工程     | 完成提示音 — ChimePlayer（AudioTrack 合成 C5→E5 钟声）    | ✅ 完成 | 0.9s，5% 起音 + exp 衰减，USAGE_NOTIFICATION |
| 工程     | Settings 声音开关联动 ChimePlayer.play                     | ✅ 完成 | 用户可在设置里关                         |
| 主屏     | 自定义时长对话框（Slider 5-90 分钟，1 分钟步进）            | ✅ 完成 | 点"自定义"chip 弹 AlertDialog          |
| 专注     | Running 屏 PAUSED 态「↻ 重新开始」按钮                      | ✅ 完成 | 补回 PRD §3.2B 第三个动作               |
| 工程     | 启动时 Logcat 打印 DataStore + Room 实际路径（FlowInit tag）| ✅ 完成 | 方便对照 backup_rules.xml 验证         |
| 部署     | SETUP.md §6 加「DataStore 备份恢复验证」流程                | ✅ 完成 | bmgr backupnow + 重装回归               |

---



---

## V0.5.0 增量（2026-08-25）— 焦点屏分类两行 + 时长 30 秒起步

按用户 2026-08-25 反馈（详见 docs/用户需求功能代办.md）：

### 一、已完成功能（V0.5.0 增量）

| 模块     | 功能点                                                       | 状态   | 备注                                 |
| -------- | ------------------------------------------------------------ | ------ | ------------------------------------ |
| 主屏     | 焦点屏分类从 LazyRow 横向改成 2 行 3 列（写作/编程/阅读 一行，学习/设计/其他 一行） | ✅ 完成 | CategoryRow 去掉 LazyRow，改 Column + 2 个 Row 各 3 chip，chip 用 Modifier.weight(1f) 平分宽度 |
| 主屏     | CategoryChip 加 modifier 参数，Row 头接 modifier 透传       | ✅ 完成 | -                                    |
| 主屏     | 时长最短从 30 秒开始（Slider 0.5..90 分钟，0.5 步进）         | ✅ 完成 | FocusViewModel.duration: Int -> Float(0.5f..90f)；CustomDurationDialog Slider valueRange 改 0.5f..90f，steps=178 |
| 主屏     | TimerController 新增 prepareFocusSeconds(task, cat, seconds)  | ✅ 完成 | 旧 prepareFocus(minutes) 保留兼容     |
| 主屏     | CustomDurationDialog / DurationRow 显示策略：< 1 分钟显示 Xs，>= 1 分钟显示 Xm | ✅ 完成 | -                                    |

公开版本号维持 V0.1.0，V0.5.0 为内部迭代节点。

---

## V0.5.1 增量（2026-08-26）— 串行状态机（SessionStateMachine 独立文件）

按用户 2026-08-26 反馈：屏切换按串行走，timer 状态变更跟切屏完全同步。

### 一、已完成功能（V0.5.1 增量）

| 模块     | 功能点                                                       | 状态   | 备注                                 |
| -------- | ------------------------------------------------------------ | ------ | ------------------------------------ |
| 工程     | 新文件 `ui/session/SessionStateMachine.kt`                  | ✅ 完成 | 封装 4 个动作 + 3 个辅助（pause/resume/restart），viewModel 不再直接碰 TimerController |
| 状态机   | start -> running (startFocus)                                | ✅ 完成 | FocusViewModel.prepareAndStart 调 stateMachine.startFocus |
| 状态机   | running -> start (abandon)                                   | ✅ 完成 | 保留 remainingSeconds 不归零（满足"保持当前时间不动"），phase 改 ABANDONED |
| 状态机   | running -> rest (beginRest)                                  | ✅ 完成 | 自然完成时 Vibrate+Chime 同步触发，DB 异步保存 |
| 状态机   | rest -> start (skipRest)                                     | ✅ 完成 | 保留 remainingSeconds 不归零（满足"保持当前画面不动"），phase 改 IDLE |
| Timer    | TimerController.finishRest 改：保留 remainingSeconds          | ✅ 完成 | 不再清空 State()，只 cancelTick + phase=IDLE              |
| Rest 屏  | RestScreen 加 LaunchedEffect 监听 RESTING+0 自动 onFinish    | ✅ 完成 | 自然休息停止后自动从 rest 切到 start                       |
| 业务     | AppContainer 加 sessionStateMachine 单例                     | ✅ 完成 | -                                    |
| 业务     | FlowViewModelFactory 注入 stateMachine 给 Focus/Running VM   | ✅ 完成 | -                                    |

### 二、本期调测经验（V0.5.1 增量）

- **问题**：之前切屏时 timer 状态变更（reset/abandon/startRest/finishRest）跟 nav.navigate 是两条并行路径，旧屏能"看到"已被 reset 后的 state，视觉上有 bug。
  **解决**：把 timer 状态变更和切屏拆到同一 click 事件里同步执行（一个串行），不再用异步 viewModelScope.launch 包住 timer 操作。

公开版本号维持 V0.1.0，V0.5.1 为内部迭代节点。

---

## V0.5.2 增量（2026-08-26）— 焦点时长预设 15/25/40/60 + 休息屏绿叶版 + 记录屏改版

按用户 2026-08-26 反馈（详见 docs/用户需求功能代办.md）：

### 一、已完成功能（V0.5.2 增量）

| 模块     | 功能点                                                       | 状态   | 备注                                 |
| -------- | ------------------------------------------------------------ | ------ | ------------------------------------ |
| 主屏     | DurationRow 时长预设改成 15/25/40/60（去掉 45 改 40）        | ✅ 完成 | -                                    |
| 主屏     | 修复 bug：4 个固定值按钮之前都调 onCustomClick() 弹自定义对话框 | ✅ 完成 | 改为 onPresetSelect(v) 直接设时长；"自定义" chip 仍走 onCustomClick() |
| 休息     | RestScreen 改版（按 prototype 图）                            | ✅ 完成 | 顶部 REST TIME + 休息一下 + 右上"跳过"；中部绿叶 Icons.Filled.Eco 56dp + 呼吸动效（scale 0.88->1.08 + alpha 0.65->1.0，3000ms 循环）+ mmss 倒计时；中段 3 行提示语；底部勾选 + 已完成一次专注 + 副标 {category} {minutes} 分钟 |
| 休息     | "跳过"按钮调 onFinish（同自然完成路径）                     | ✅ 完成 | 走 V0.5.1 串行状态机                                |
| 记录     | HistoryScreen 改版（按 prototype 图）                        | ✅ 完成 | 顶部标题"记录" + 右上统计图表图标（-> Stats）；今日 3 KPI（完成次数 / 总时长 / 连续天数）；本周 label + 次数 时长 + +X% 趋势 chip + 7 根柱状图 Mon-Sun 排（今天柱子深色）；最近完成 label + 查看全部 + 列表（分类图标 + 任务 + 副标 + HH:mm + 刚刚/今天/昨天） |
| 记录     | HistoryViewModel 加回 todayCount/todayMinutes/streakDays     | ✅ 完成 | weekBars 重排为 Mon-Sun（按 today.dayOfWeek 旋转）                  |
| 记录     | HistoryViewModel 加 prevWeekCount/prevWeekMinutes 算 +X% 趋势 | ✅ 完成 | 用 StatsCalculator.lastNDays(completed, 7, today.minusDays(7))      |
| 统计     | StatsCalculator 加回 lastNDays(sessions, days, today)         | ✅ 完成 | V0.4.0 加的，回退时丢了，V0.5.2 补回                                |
| 工程     | 字符串资源：rest_skip / rest_label_focus / rest_hint_lines / rest_summary_title / history_weekly_section / history_week_summary / history_see_all / history_recent_done / history_trend_count / history_relative_* | ✅ 完成 | 10 个新 string                                            |

公开版本号维持 V0.1.0，V0.5.2 为内部迭代节点。

## 二、下一步工作计划

| 优先级 | 计划功能                                                     | 预计耗时 | 依赖/备注                            |
| ------ | ------------------------------------------------------------ | -------- | ------------------------------------ |
| P0     | UI 优化 — 移除 in-app StatusBar（Android 系统栏已自带）     | 0.5 天   | 改 9 屏 + BottomNavBar systemBarsPadding |
| P0     | ~~修复 Android 11+ 备份/恢复中 DataStore 兼容性~~            | 0.5 天   | ✅ 本迭代完成（V0.3.0）             |
| P1     | ~~后台保活（Doze 模式下计时器继续）~~                        | 2 天     | ✅ 本迭代完成，见下表               |
| P1     | ~~重新设计 Running 屏的「重新开始」入口~~                      | 0.5 天   | ✅ 本迭代完成（V0.3.0）             |
| P1     | ~~P1 视觉打磨（每日目标/热力图入口/导出按钮）~~              | 1 天     | ✅ 本迭代完成，见下表               |
| P2     | P2 白噪音 / Apple Watch / 桌面小组件 评估                    | -        | 待需求                               |
| P2     | Release APK 签名 + Play Internal 上架                        | 1 天     | 用户决定是否上架                     |
| P2     | ProGuard / R8 规则打磨                                       | 0.5 天   | ksp 后的混淆                         |

---

## 三、本期调测经验

### 调试经验

- **问题**：首次编译报 `Too many arguments for HistoryViewModel`
  **解决**：精简 `HistoryViewModel` 时删了 `SettingsRepository` 参数但漏改 `FlowViewModelFactory.kt`，对齐构造函数签名。

- **问题**：`Unresolved reference 'Row' / 'width'` 编译报错
  **解决**：清理各屏 `private val Unused = ...` 占位行时连带删了 `layout` 包的 import，逐一补回 `import androidx.compose.foundation.layout.{Row, width}`。

- **问题**：`Unresolved reference 'flow'` 报错
  **解决**：`FlowNavHost.kt` 写错了包名 `com.liuflow.app.ui.flow.FocusScreen`，正确应为 `com.liuflow.app.ui.focus.FocusScreen`。

- **问题**：`Condition type mismatch: inferred type is 'PointerInputChange?' but 'Boolean' was expected`
  **解决**：`waitForUpOrCancellation()` 返回 `PointerInputChange?`（取消时为 null），把 `released && ...` 改成 `released != null && ...` 区分正常释放与手势取消。

- **问题**：原 FAB 在 edge-to-edge 模式下被系统底部导航条遮挡，模拟器上看不到
  **解决**：用圆环接管开始/暂停/停止，符合 PRD「极致克制」原则，同时天然支持长按扫光手势，无须额外控件。

- **问题**：AS 内置 AOSP 镜像没有 Google Play，但需要确保 App 仍能完整运行
  **解决**：确认 `AndroidManifest.xml` 未声明任何 GMS 依赖，纯 Room + DataStore 全本地化，AOSP 镜像直接装即可跑。

### AI 提问记录（给 AI 的提问 & 遇到的问题）

| 提问时间 | 我的提问                                                     | 遇到的问题/背景                                       | AI 给出的方案                                                | 是否解决 | 备注/最终方案                              |
| -------- | ------------------------------------------------------------ | ----------------------------------------------------- | ------------------------------------------------------------ | -------- | ------------------------------------------ |
| 8/23     | "请帮我做 Android 应用"                                      | 本机无 JDK / AS / SDK，要从零搭                       | 给出完整工程（46 个 Kotlin 文件，~4100 行）+ SETUP.md 部署指南 | ✅ 解决   | -                                          |
| 8/23     | "AS 同步时系统镜像选 Google Play / Google APIs / AOSP？"     | 不知道三者的区别                                       | 推荐 AOSP（用户明确说不上架）                                | ✅ 解决   | -                                          |
| 8/23     | "模拟器里没看到我的应用程序"                                 | 不知道如何编译并安装到模拟器                           | 点 ▶ Run 让 AS 编译 + 推送到模拟器                            | ✅ 解决   | -                                          |
| 8/23     | "开始控件在安卓上不显示"                                     | FAB 被 edge-to-edge 系统栏挡了                        | 改为点击圆环启动；暂停/停止也走圆环；长按 3s 红色扫光放弃    | ✅ 解决   | 用户驱动，AI 落地                          |
| 8/23     | "HistoryViewModel 构造参数太多"                              | 编译报错                                               | 同步 factory 调用                                             | ✅ 解决   | -                                          |
| 8/23     | "Row / width 引用不到"                                       | 编译报错                                               | 补 import                                                     | ✅ 解决   | -                                          |
| 8/23     | "ui.flow 包名找不到"                                         | 编译报错                                               | 改为 ui.focus                                                 | ✅ 解决   | -                                          |
| 8/23     | "PointerInputChange? 与 Boolean 类型不匹配"                  | waitForUpOrCancellation 返回 nullable                  | 改为 `released != null` 判断                                  | ✅ 解决   | -                                          |

---

## 四、附：交付物清单

- `android/` — Android 工程根目录
  - `app/src/main/java/com/liuflow/app/` — Kotlin 源码 50 个文件 / 6445 行（V0.5.2 截至）
  - `app/src/main/res/` — 资源（主题、字符串、图标、备份规则）
  - `app/src/test/` — JUnit 单测
  - `SETUP.md` — 环境部署指南
  - `README.md` — 项目说明
- `prototype/` — HTML 原型（已存在，本期未改动）
- `docs/prd.md` — 产品需求文档（已存在，本期未改动）
