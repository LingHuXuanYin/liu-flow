# 用户需求功能代办

> **规则**：每次用户提改动需求都先加到本文件，完成后由 Mavis 打勾 `[x]` 并附 commit 号。
> 本文档由 Mavis 维护；用户授权 commit 后再同步到远端。

---

## 已完成 / 已关闭（按版本倒序）

### V0.6.0 身份认证重写 + 登出 + 版本号配置 + 注册单页化（2026-08-26 完成）

- [x] **#10 修主流程登录 NPE：`UserDto.getUid()` on null reference**
  - 来源：用户 2026-08-26 反馈
  - 现状：CloudBase `/auth/v1/signin` 实际响应字段是 `sub`（不是嵌套 `user.uid`），Gson 反序列化时 `user` 字段为 null
  - 修法：按文档示范砍掉整套 V0.1.0 加的 AuthRepository/AuthSession/User 封装，主流程用 `Map<String, Any>` 直接读 `sub` 字段
  - 优先级：P0
  - 状态：✅ 已修复（详见 V0.6.0 迭代记录 §1.1）

- [x] **#11 身份认证模块按接入指引 2026 文档示范重写**
  - 来源：用户 2026-08-26 反复强调"必须用 `signUpWithEmailCode` 这个区注册"
  - 现状：V0.1.0 加了大量"生产加强"封装（AuthRepository / AuthTokenStore / AuthInterceptor / EncryptedSharedPreferences / 嵌套 User / 21 个 AuthError 枚举）
  - 目标：完全按 `docs/腾讯云开发接入指引2026.md` §1.1.2 + §1.1.4 文档示范风格
    - 一个 `CloudBaseAuthApi`（OkHttp + Gson 通用客户端，不挂 AuthInterceptor）
    - 一个 `AuthManager`（SharedPreferences 存 token + 业务方法）
    - 错误用 `kotlin.Result<T>` + `AuthException(code, message)`，不自定义 sealed class
    - 错误消息直接取自接口响应的 `message` 字段
  - 涉及：
    - 新建 `auths/data/AuthManager.kt`
    - 简化 `auths/data/CloudBaseAuthApi.kt`（去掉 AuthInterceptor 引用 + setRefresher）
    - 删除 8 个 V0.1.0 文件（备份到 `docs/_deprecated_auths_v020/`）
  - 优先级：P0
  - 状态：✅ 已完成

- [x] **#12 Me 页加「退出登录」按钮（清 token + 跳登录）**
  - 来源：用户 2026-08-26 反馈
  - 现状：MeScreen 有登出 Row 但 `onSignOut` 回调空实现，点了没反应；判定用 `email.isNotBlank()` 但 /signin 响应 email 可选
  - 目标：
    - MeScreen 直接调 `viewModel.signOut()`（不依赖 onSignOut 回调）
    - 判定用 `isLoggedIn: StateFlow<Boolean>`（看本地 token 存在）
    - 副标题 fallback：email → username → sub
    - 脱敏函数 `maskAccount`：邮箱 `ye****@example.com`，sub `uid:****`
  - 涉及：`MeViewModel.kt` / `MeScreen.kt`
  - 优先级：P0
  - 状态：✅ 已完成

- [x] **#13 版本号集中到 `gradle.properties`**
  - 来源：用户 2026-08-26 反馈
  - 现状：`app/build.gradle.kts` 的 `versionName = "0.1.0"`
  - 目标：
    - `gradle.properties` 加 `APP_VERSION_NAME` / `APP_VERSION_CODE`
    - `app/build.gradle.kts` 读这两个属性
    - debug 仍走 `versionNameSuffix = "-debug"` 自动拼后缀
  - 涉及：`gradle.properties` / `app/build.gradle.kts`
  - 优先级：P1
  - 状态：✅ 已完成（0.6.0）

- [x] **#14 注册 UI 单页一体化**
  - 来源：用户 2026-08-26 反馈 "我实际注册时候是只有一个界面"
  - 现状：V0.6.0 早期实现的 2 step 跳路由版本（`AuthRoutes.SignupVerify`）
  - 目标：
    - 单页：邮箱 + 账号 + 密码 + 确认密码 + 验证码输入行 + 「获取验证码」按钮 + 「注册」按钮
    - 验证码输入行：`Row { OutlinedTextField(weight=1f) + Button }` 左右并排
    - 「获取验证码」按钮：60s 倒计时变 `${countdown}s`，0 时变 "获取验证码"
    - `verificationId` / `verificationToken` 全在 `SignupViewModel` 内部流转，**不暴露给 UI**
  - 涉及：`SignupViewModel.kt`（去掉 step 状态机）/ `SignupScreen.kt`（单页布局）
  - 优先级：P0
  - 状态：✅ 已完成

- [x] **#15 修一堆编译错（V0.6.0 早期迭代引入）**
  - 来源：用户 2026-08-26 反馈
  - 涉及：
    - `Modifier.align(Alignment.CenterHorizontally)` 是 `ColumnScope` 扩展方法，顶层 Composable 不可用 → 改 `Modifier.fillMaxWidth().wrapContentWidth(Alignment.CenterHorizontally)`
    - `wrapContentWidth` 缺 import
    - `VerificationScreen.kt` deprecated stub 缺 `NavController` import
    - `ApiResult.Failure` 字段从 `error: AuthError` 改 `message: String` 后，DebugViewModel 10 处 `.error.message` 引用 → 批量改成 `.message`
  - 优先级：P0
  - 状态：✅ 已完成

### V0.5.2 焦点时长预设 15/25/40/60 + 休息屏绿叶版 + 记录屏改版（2026-08-26 完成）

- [x] **#7 03 休息屏 UI 改版（REST TIME + 绿叶呼吸 + 跳过 + 摘要卡）** — commit 1bd3621
  - 来源：用户 2026-08-26 反馈（附图）
  - 现状：V0.5.1 RestScreen 用老版本 FocusRing 圆环 + "BREAK" 状态 + "结束" 按钮
  - 目标布局（图样）：
    - 顶部：`REST TIME`（小字 letter-spacing 2sp）+ `休息一下` 大字 + 右上 `跳过` 按钮
    - 中部：绿叶 `Icons.Filled.Eco`（56dp，呼吸动效 scale 0.88→1.08 + alpha 0.65→1.0，3000ms 循环）+ 大字时间 `mmss(timerState.remainingSeconds)`（displayLarge mono）
    - 中段：3 行提示语（`站起来走一走` / `看 5 米外的绿植` / `让眼睛休息一下`）
    - 底部：勾选 + `已完成一次专注` + 副标 `${category 名称} · ${plannedMinutes} 分钟`
  - 涉及：
    - `ui/rest/RestScreen.kt` 完全重写
    - `res/values/strings.xml` 加 `rest_skip` / `rest_label_focus` / `rest_hint_lines` / `rest_summary_title` 4 个 string
  - 跳过按钮调 `onFinish`（同自然完成路径），保持 V0.5.1 串行状态机
  - 优先级：P0
  - 状态：✅ 完成

- [x] **#8 04 记录页 UI 改版（按 prototype 图）** — commit 1bd3621
  - 来源：用户 2026-08-26 反馈（附图）
  - 现状：V0.5.1 HistoryScreen 用"今日卡 + 7 天柱图 + 最近列表"布局
  - 目标布局（图样）：
    - 顶部：标题"记录" + 右上统计图表小图标（→ Stats 屏）
    - 本周：label "本周" + "次数 · 时长" + 右上 "+X%" 趋势 chip（vs 上周同窗口） + 7 根柱状图（一/二/.../日，按 Mon-Sun 排，今天柱子深色）
    - 最近完成：label + 右上"查看全部" 链接 + 列表（分类图标 + 任务标题 + "写作 · 25 分钟" 副标 + HH:mm 时间 + 刚刚/今天/昨天 相对时间）
  - 涉及：
    - `ui/history/HistoryScreen.kt` 完全重写
    - `ui/history/HistoryViewModel.kt` 加 `prevWeekCount / prevWeekMinutes`，weekBars 重排为 Mon-Sun
    - `data/stats/StatsCalculator.kt` 加 `lastNDays(sessions, days, today)`（V0.4.0 已有但回退时丢了）
    - `res/values/strings.xml` 加 5 个 string
  - 优先级：P0
  - 状态：✅ 完成

- [x] **#9 01 焦点屏时长预设修复（4 个固定值按钮 clickable 之前都调 onCustomClick()）** — commit 1bd3621
  - 来源：用户 2026-08-26 反馈
  - 现状：点 15/25/40/60 弹自定义对话框
  - 目标：4 个固定值按钮直接设时长；"自定义" chip 仍弹对话框
  - 涉及：FocusScreen DurationRow 加 onPresetSelect 参数
  - 优先级：P0
  - 状态：✅ 完成

### V0.5.1 串行状态机（抽出 SessionStateMachine 独立文件）（2026-08-26 完成）

- [x] **#6 V0.5.1 串行状态机（抽出 SessionStateMachine 独立文件）** — commit dca39cd
  - 来源：用户 2026-08-26 反馈
  - 现状：状态机逻辑散落在 4 个文件
  - 目标：抽到 `ui/session/SessionStateMachine.kt`，3 态 4 动作，timer 状态变更跟切屏完全同步
  - 4 动作：startFocus / abandon / beginRest / skipRest
  - TimerController.finishRest 改：保留 remainingSeconds
  - RestScreen 加 LaunchedEffect 监听 RESTING+0 自动 onFinish
  - 优先级：P0
  - 状态：✅ 完成

### V0.5.0 焦点屏分类两行 + 时长 30 秒起步（2026-08-25 完成）

- [x] **#1 焦点屏取消右下角"开始" UI**
  - 来源：用户 2026-08-25 反馈
  - 现状：V0.1.0 已无 FAB（V0.4.0 加的回退后没了），本轮仅做语义确认：明确"Focus 屏不再加 Extended FAB"，由圆环 tap 继续承担开始入口
  - 优先级：P0
  - 状态：✅ 仅做语义确认

- [x] **#2 焦点屏分类从一整行改两行**（V0.5.0 完成，commit 7d31cdb）
  - 现状：CategoryRow 用 LazyRow 横向滚动
  - 目标：改成 2 行 3 列布局（写作/编程/阅读 一行，学习/设计/其他 一行）
  - 涉及：`FocusScreen.kt` CategoryRow / CategoryChip
  - 优先级：P0
  - 状态：✅ 完成

- [x] **#3 焦点屏时长最短从 30 秒开始**（V0.5.0 完成，commit 7d31cdb）
  - 现状：CustomDurationDialog 的 Slider `valueRange = 5f..90f` 5 分钟起步
  - 目标：Slider 最小值改 0.5 分钟（30 秒），按 0.5 分钟步进；显示策略：< 1 分钟显示 "Xs"，≥ 1 分钟显示 "Xm"
  - 涉及：`FocusScreen.kt` CustomDurationDialog、DurationRow 自定义 chip 显示、FocusViewModel 默认值
  - 优先级：P0
  - 状态：✅ 完成

### 未来需求池（待用户确认）

（暂无）
