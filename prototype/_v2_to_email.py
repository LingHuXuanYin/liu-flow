"""
V0.2.0 PRD 大改：手机号+短信验证码 → 邮箱+密码+图像验证码
策略：全局关键短语替换 + 关键大段整体重写
"""

import re
from pathlib import Path

PATH = Path(r"Q:\large_program\liu-flow\docs\prd_v0.2.0.md")
text = PATH.read_text(encoding="utf-8")

# ============================================================
# Step 1: 关键短语全局替换（基础）
# ============================================================
REPLACEMENTS = [
    # 顶部 Changelog
    ("V0.1.0 把「不登录、零启动、全部 localStorage」当作品牌核心价值之一。**V0.2.0 正式放弃这一哲学**，转向「手机号强制登录 + 云端同步」。",
     "V0.1.0 把「不登录、零启动、全部 localStorage」当作品牌核心价值之一。**V0.2.0 正式放弃这一哲学**，转向「邮箱+密码强制登录 + 云端同步」。"),
    ("验证码即密码，免去「忘记密码」一类设计包袱",
     "邮箱密码是 CloudBase 原生最简方案，图像验证码由风控触发"),
    ("第 11 章：用户系统（手机号 + 短信验证码登录）",
     "第 11 章：用户系统（邮箱 + 密码登录 + 图像验证码风控）"),
    ("强制登录换取云端**：用 1 次验证码的代价，换取换手机不丢数据的安心",
     "强制登录换取云端**：用邮箱+密码的标准账号体系，换取换手机不丢数据的安心"),

    # 核心功能表
    ("| **用户登录（V0.2.0 新增）** | 手机号 + 短信验证码 | 腾讯云 SMS + CloudBase |",
     "| **用户登录（V0.2.0 新增）** | 邮箱 + 密码（图像验证码风控） | CloudBase Auth 内置 |"),
    ("| 找回密码 — V0.2.0 用验证码重置代替 |",
     "| 找回密码 — V0.2.0 邮箱链接重置 |"),

    # 首次使用流程
    ("→ 直接显示手机号输入页",
     "→ 直接显示邮箱 + 密码输入页"),
    ("登录页：输入手机号 → 发送验证码 → 输入 6 位验证码",
     "登录页：输入邮箱 + 密码 → 风控触发时显示图像验证码 → 登录"),

    # 页面清单
    ("| **登录（V0.2.0 新增）** | `login.html` | 启动 / 退出登录 | 手机号 + 验证码 |",
     "| **登录（V0.2.0 新增）** | `login.html` | 启动 / 退出登录 | 邮箱 + 密码 |"),
    ("│  圆环+任务│              │ 手机号+验证│",
     "│  圆环+任务│              │ 邮箱+密码│"),
]

for old, new in REPLACEMENTS:
    text = text.replace(old, new)


# ============================================================
# Step 2: 整体重写 §11.1 - 11.4
# ============================================================
old_11 = """### 11.1 用户对象

```typescript
interface User {
  uid: string;              // CloudBase 生成的 UUID，主键
  phone: string;            // 11 位中国大陆手机号，不带 +86 前缀
  createdAt: number;        // 首次注册时间戳
  lastLoginAt: number;      // 最近登录时间戳
  nickname?: string;        // 用户昵称（可选）
  avatarUrl?: string;       // 头像 URL（可选，V0.2.0 暂不开放上传）
}
```

**身份凭证**：
- 唯一凭证：手机号
- 登录方式：手机号 + 6 位短信验证码（一次性，5 分钟有效）
- **不**存密码

**Token**：
- 类型：JWT（HS256）
- 颁发方：CloudBase 身份认证
- 有效期：2 小时
- 续期：剩余有效期 < 10 分钟时自动续期"""

new_11 = """### 11.1 用户对象

```typescript
interface User {
  uid: string;              // CloudBase 生成的 UUID，主键（对应 JWT sub）
  email: string;            // 标准邮箱（RFC 5322 格式）
  username?: string;        // 可选：用户绑定的 username（登录备用）
  createdAt: number;        // 首次注册时间戳
  lastLoginAt: number;      // 最近登录时间戳
  nickname?: string;        // 用户昵称（可选）
  avatarUrl?: string;       // 头像 URL（可选，V0.2.0 暂不开放上传）
}
```

**身份凭证**：
- 唯一凭证：邮箱（注册时必填，登录主用）
- 可选凭证：username（CloudBase 用户名规则，登录备用）
- 登录方式：邮箱 + 密码（V0.2.0 强密码：8-32 位字母 + 数字）
- 图像验证码：CloudBase 风控触发（连续登录失败 3-5 次后必须）

**Token**：
- 类型：JWT（HS256）
- 颁发方：CloudBase 身份认证
- 有效期：2 小时
- 续期：剩余有效期 < 10 分钟时自动续期"""

text = text.replace(old_11, new_11)


# ============================================================
# Step 3: 整体重写 §11.2.1 主流程
# ============================================================
old_1121 = """#### 11.2.1 主流程

```
[用户]                 [App]                    [CloudBase]              [腾讯云 SMS]
  │                     │                            │                        │
  │ 输入手机号          │                            │                        │
  ├───────────────────→│ 校验手机号格式              │                        │
  │                     ├──────────── 发送验证码 ───→│ 调用 sendCode 云函数    │
  │                     │                            ├──────── 下发短信 ─────→│
  │                     │                            │                        │
  │ 收到 6 位验证码     │                            │                        │
  │←──────────────────────────────────────────────────────────────────────────┤
  │                     │                            │                        │
  │ 输入 6 位验证码      │                            │                        │
  ├───────────────────→│ 调用 verifyLogin 云函数      │                        │
  │                     ├──────────── 校验 ─────────→│ 校验验证码              │
  │                     │                            ├──── 创建/查询用户 ────→│
  │                     │                            │                        │
  │                     │←──────── 返回 JWT ─────────┤                        │
  │←── 登录成功 ────────│ 写入安全存储                 │                        │
  │                     │ 触发数据拉取                │                        │
  ↓                     ↓                            ↓                        ↓
```"""

new_1121 = """#### 11.2.1 主流程（邮箱 + 密码 + 图像验证码风控）

```
[用户]                 [App]                    [CloudBase Auth v2]
  │                     │                            │
  │ 输入邮箱 + 密码      │                            │
  ├───────────────────→│ 校验邮箱格式                │
  │                     ├──── POST /auth/v1/signin ──→│
  │                     │   {email, password}        │
  │                     │                            │
  │                     │←─ {success: true,         ─┤
  │                     │     access_token,          │
  │                     │     refresh_token,         │
  │                     │     user: {uid, email}}    │
  │                     │                            │
  │←── 登录成功 ────────│ 写入安全存储                │
  │                     │ 触发数据拉取                │
  ↓                     ↓                            ↓

─── 失败流程（密码错误 3-5 次后）─────────────────────────

  │ 输入邮箱 + 错误密码  │                            │
  ├───────────────────→│                            │
  │                     ├──── POST /auth/v1/signin ──→│
  │                     │   {email, password}        │
  │                     │                            │
  │                     │←─ error.code =             ─┤
  │                     │   CAPTCHA_REQUIRED         │
  │                     │   (返回 captcha_url)        │
  │                     │                            │
  │ 弹出图像验证码弹窗   │                            │
  │ 看到 4 位字符图片    │                            │
  │ 用户输入图片字符     │                            │
  │                     │                            │
  │                     ├─ POST HTTP 图形验证码 API ─→│
  │                     │   (拿 captcha_token)       │
  │                     │                            │
  │                     ├─ POST /auth/v1/signin ────→│
  │                     │   {email, password,        │
  │                     │    captcha_token}           │
  │                     │                            │
  │                     │←─ {success: true,         ─┤
  │                     │     access_token, ...}     │
  │←── 登录成功 ────────│                            │
  ↓                     ↓                            ↓
```

**关键点**：
- 正常登录无需图像验证码，**只在密码错 3-5 次后风控触发**
- 验证码图片由 CloudBase 通过 HTTP API 提供，客户端渲染
- 验证通过后拿到 `captcha_token`，再次登录时附带即可
- 完全 CloudBase 原生，不写云函数"""

text = text.replace(old_1121, new_1121)


# ============================================================
# Step 4: 整体重写 §11.2.2 限流规则
# ============================================================
old_1122 = """#### 11.2.2 验证码限流规则

| 维度 | 阈值 | 超出处理 |
|------|------|---------|
| 同一手机号 | 1 分钟 1 次 | 提示「请稍候」+ 倒计时 |
| 同一手机号 | 1 小时 5 次 | 提示「请求过于频繁，请稍后再试」 |
| 同一手机号 | 1 天 10 次 | 提示「已达今日上限，请明天再试」 |
| 同一 IP | 1 小时 20 次 | 触发风控 → 验证码图灵测试 |
| 验证码错误次数 | 连续 5 次 | 当前验证码作废，需重新发送 |"""

new_1122 = """#### 11.2.2 限流与风控规则

| 维度 | 阈值 | 超出处理 |
|------|------|---------|
| 同一邮箱 | 1 分钟 5 次 | 提示「请稍候」+ 倒计时 |
| 同一邮箱 | 1 小时 20 次 | 提示「请求过于频繁，请稍后再试」 |
| 同一 IP | 1 小时 30 次 | 触发风控 → 强制图像验证码 |
| 密码错误次数 | 连续 3-5 次 | 触发 `CAPTCHA_REQUIRED` → 必须输入图像验证码 |
| 同一账号累计失败 | 1 天 10 次 | 临时锁定 1 小时（CloudBase 风控默认） |
| 注册邮箱已存在 | - | 提示「该邮箱已注册，请直接登录」 |"""

text = text.replace(old_1122, new_1122)


# ============================================================
# Step 5: 整体重写 §11.2.3 错误处理
# ============================================================
old_1123 = """#### 11.2.3 错误处理

| 错误码 | 含义 | 用户提示 |
|--------|------|---------|
| `PHONE_INVALID` | 手机号格式错误 | 「请输入正确的手机号」 |
| `CODE_EXPIRED` | 验证码过期（>5 分钟） | 「验证码已过期，请重新获取」 |
| `CODE_WRONG` | 验证码错误 | 「验证码错误，还有 N 次机会」 |
| `RATE_LIMIT` | 触发限流 | 「请求过于频繁，请稍后再试」 |
| `SMS_FAILED` | 短信下发失败 | 「短信发送失败，请重试」 |
| `NETWORK_ERROR` | 网络异常 | 「网络异常，请检查网络后重试」 |
| `SERVER_ERROR` | 服务异常 | 「服务暂时不可用，请稍后再试」 |"""

new_1123 = """#### 11.2.3 错误处理

| 错误码 | 含义 | 用户提示 |
|--------|------|---------|
| `INVALID_EMAIL` | 邮箱格式错误 | 「请输入正确的邮箱」 |
| `USER_NOT_FOUND` | 邮箱未注册 | 「该邮箱未注册，请先注册」 |
| `USER_ALREADY_EXISTS` | 邮箱已注册 | 「该邮箱已注册，请直接登录」 |
| `INVALID_PASSWORD` | 密码错误 | 「密码错误，还有 N 次机会」 |
| `PASSWORD_TOO_WEAK` | 注册密码强度不够 | 「密码需 8-32 位，含字母和数字」 |
| `RATE_LIMIT_EXCEEDED` | 触发限流 | 「请求过于频繁，请稍后再试」 |
| `CAPTCHA_REQUIRED` | 风控触发，需图像验证码 | 「请输入图像验证码」 |
| `CAPTCHA_WRONG` | 图像验证码错误 | 「图像验证码错误，请重试」 |
| `ACCOUNT_LOCKED` | 账户临时锁定 | 「尝试次数过多，请 1 小时后再试」 |
| `NETWORK_ERROR` | 网络异常 | 「网络异常，请检查网络后重试」 |
| `SERVER_ERROR` | 服务异常 | 「服务暂时不可用，请稍后再试」 |"""

text = text.replace(old_1123, new_1123)


# ============================================================
# Step 6: 整体重写 §11.3 注册
# ============================================================
old_113 = """### 11.3 注册

**首次登录即注册**：
- 手机号在 users 表中不存在 → 自动创建记录
- 不需要单独的「注册」步骤
- 不需要「同意服务条款」按钮（登录按钮下方以小字标注）"""

new_113 = """### 11.3 注册

**CloudBase 邮箱注册流程**：
1. 用户在登录页点击「没有账号？立即注册」→ 切换到注册页
2. 输入邮箱 + 密码（强度 8-32 位字母 + 数字）
3. 客户端调 `auth.signUp({ email, password, username?: string })`
4. CloudBase 自动创建用户，返回 `access_token`（**注册成功即自动登录**）
5. 不需要「同意服务条款」单独步骤（登录按钮下方以小字标注）

**重复注册处理**：
- 邮箱已存在 → 报错 `USER_ALREADY_EXISTS`
- 客户端提示「该邮箱已注册，请直接登录」并自动跳回登录页"""

text = text.replace(old_113, new_113)


# ============================================================
# Step 7: §11.4 切换账号 - 局部
# ============================================================
text = text.replace(
    "退出当前账号 → 回到登录页 → 输入新手机号登录",
    "退出当前账号 → 回到登录页 → 输入新邮箱登录"
)


# ============================================================
# Step 8: 整体重写 §12.1 选型
# ============================================================
old_121 = """### 12.1 技术选型总览

| 层级 | 选型 | 理由 |
|------|------|------|
| **短信** | 腾讯云 SMS | 国民级到达率，验证码类短信支持，0.045 元/条起 |
| **后端 BaaS** | 腾讯云 CloudBase | 一体化：身份认证 + 数据库 + 云函数 + 存储，对齐 Supabase 心智，RLS 天然支持 |
| **数据库** | CloudBase PostgreSQL | 关系型，事务、索引、行级权限（RLS）开箱即用 |
| **后端函数** | CloudBase 云函数（Node.js 18） | 按调用付费，0→N 自动扩缩容 |
| **存储** | CloudBase 云存储 | 用于头像、备份导出文件（V0.2.0 暂不启用） |
| **CDN** | CloudBase 静态托管 | V0.2.0 暂不部署 Web 端，预留 |"""

new_121 = """### 12.1 技术选型总览

| 层级 | 选型 | 理由 |
|------|------|------|
| **身份认证** | CloudBase Auth v2 | 内置邮箱+密码登录、图像验证码风控、JWT 颁发，0 部署 |
| **后端 BaaS** | 腾讯云 CloudBase | 一体化：身份认证 + 数据库 + 存储，对齐 Supabase 心智，RLS 天然支持 |
| **数据库** | CloudBase PostgreSQL | 关系型，事务、索引、行级权限（RLS）开箱即用 |
| **后端函数** | **不部署** | V0.2.0 客户端直连数据库 + RLS，0 后端代码 |
| **存储** | CloudBase 云存储 | 用于头像、备份导出文件（V0.2.0 暂不启用） |
| **CDN** | CloudBase 静态托管 | V0.2.0 暂不部署 Web 端，预留 |"""

text = text.replace(old_121, new_121)


# ============================================================
# Step 9: 整段删除 §12.2（短信服务）并替换为"用户名密码 + 图像验证码"
# ============================================================
old_122 = """### 12.2 腾讯云 SMS 短信服务

#### 12.2.1 计费方案

| 套餐 | 套餐售价 | 套餐内单价 | 适用 |
|------|---------|-----------|------|
| 1 万条 | ¥470 | 0.047 元/条 | 试用 |
| 10 万条 | ¥4500 | 0.045 元/条 | 独立项目起步 |
| 50 万条 | ¥22500 | 0.045 元/条 | 中型项目 |
| 100 万条 | ¥43000 | 0.043 元/条 | 商业化产品 |
| 300 万条 | ¥123000 | 0.041 元/条 | 大型平台 |
| 自定义套餐（≥1000 条） | 自定义 | 0.041-0.050 元/条 | 灵活 |

**企业认证首开通赠 200 条**（2026-03-24 起调整），个人认证首开通赠 100 条，3 个月内有效。

**自 2026-04-01 起**部分套餐价格已按增值税调整后更新，详见官网公告。

#### 12.2.2 验证码短信模板

**模板示例**：
```
【流 Flow】您的验证码是 {1}，5 分钟内有效，请勿泄露。
```

**模板规范**：
- 必须含「验证码」字样
- 必须含有效时间说明
- 签名需在腾讯云后台单独申请
- 审核周期：1-2 个工作日

#### 12.2.3 验证码规范

- **长度**：6 位数字
- **有效期**：5 分钟
- **使用次数**：1 次（验证成功后立即失效）
- **存储**：Redis，TTL = 5 分钟（CloudBase 云函数内集成）
- **限流**：同 §11.2.2"""

new_122 = """### 12.2 CloudBase Auth v2 邮箱 + 密码

V0.2.0 不使用腾讯云 SMS（短信验证码），改用 CloudBase Auth v2 内置的「邮箱 + 密码」登录方案。**0 短信费、0 签名审核、0 模板申请**。

#### 12.2.1 邮箱规则

- 格式：标准 RFC 5322（CloudBase 自动校验）
- 唯一：每个邮箱在 CloudBase 唯一
- 注册时：邮箱 + 密码（强度 8-32 位字母 + 数字）
- 登录时：邮箱 + 密码（CloudBase 自动校验密码强度）

#### 12.2.2 密码规则

- **长度**：8-32 位
- **复杂度**：必须同时包含字母和数字
- **存储**：CloudBase 自动使用 bcrypt 哈希存储，开发者不可见明文
- **传输**：全程 HTTPS
- **找回**：V0.2.0 暂不开放「忘记密码」邮件链接（V0.3.0 规划）

#### 12.2.3 图像验证码（风控触发）

**默认行为**：
- 正常登录不需要图像验证码
- 同一邮箱密码错误 3-5 次后，CloudBase 自动返回 `CAPTCHA_REQUIRED` 错误码
- 客户端拿到 `captcha_url`，渲染图像验证码
- 用户输入图片字符后，客户端调 HTTP API 拿到 `captcha_token`
- 附带 `captcha_token` 重新发起登录请求

**优势**：
- CloudBase 内置风控，无需自研
- 不强制每次登录都输入（用户体验好）
- 失败后才触发（精准拦截恶意请求）"""

text = text.replace(old_122, new_122)


# ============================================================
# Step 10: 整体重写 §12.3 资源消耗
# ============================================================
old_123 = """### 12.3 CloudBase 资源消耗估算

#### 12.3.1 假设场景

- 注册用户：1000 人
- 日活：100 人（10%）
- 每人每天登录 1 次（消耗 1 条短信）
- 每人每天完成 3 个 Session（每次 1 次云函数调用 + 1 次数据库写入）
- 每人每天打开 5 次 App（每次 1 次云函数调用 + 0.5 次数据库读）

#### 12.3.2 月度消耗估算

| 资源 | 单价 | 月度用量 | 月度费用 |
|------|------|---------|---------|
| 短信验证码 | 0.045 元/条 | 100 × 30 = 3000 条 | ¥135 |
| CloudBase 个人版 | ¥19.9/月 | 1 套 | ¥19.9 |
| 合计 | - | - | **≈ ¥155** |

**乐观场景**（日活 20 人）：短信 ¥27 + CloudBase ¥19.9 = **≈ ¥47/月**

#### 12.3.3 套餐选型建议

| 阶段 | 套餐 | 资源点 | 月费 |
|------|------|--------|------|
| MVP（V0.2.0 验证期） | FREE 免费版 | 3000 点 | ¥0 |
| 内测（< 100 用户） | PERSONAL 个人版 | 40000 点 | ¥19.9 |
| 公测（100-1000 用户） | STANDARD 标准版 | 330000 点 | ¥179.1 |
| 商业化 | ENTERPRISE 企业版 | 1500000 点 | ¥799.2 |

**V0.2.0 推荐起手**：FREE 免费版（3000 资源点/月）+ 10 万条短信包（¥4500，可支撑 1 万次登录）。"""

new_123 = """### 12.3 CloudBase 资源消耗估算

#### 12.3.1 假设场景

- 注册用户：1000 人
- 日活：100 人（10%）
- 每人每天登录 1 次
- 每人每天完成 3 个 Session（每次 1 次数据库写入 + 偶尔 1 次读）
- 每人每天打开 5 次 App（每次 0.5 次数据库读）

#### 12.3.2 月度消耗估算

| 资源 | 单价 | 月度用量 | 月度费用 |
|------|------|---------|---------|
| 短信验证码 | - | **0**（V0.2.0 不使用） | **¥0** |
| CloudBase 个人版 | ¥19.9/月 | 1 套 | ¥19.9 |
| 合计 | - | - | **≈ ¥20/月** |

**对比 V0.2.0 PRD 原短信方案节省 ¥135/月**（短信 ¥135 + CloudBase ¥19.9 = ¥155 → 仅 CloudBase ¥19.9）。

#### 12.3.3 套餐选型建议

| 阶段 | 套餐 | 资源点 | 月费 |
|------|------|--------|------|
| MVP（V0.2.0 验证期） | FREE 免费版 | 3000 点 | ¥0 |
| 内测（< 100 用户） | PERSONAL 个人版 | 40000 点 | ¥19.9 |
| 公测（100-1000 用户） | STANDARD 标准版 | 330000 点 | ¥179.1 |
| 商业化 | ENTERPRISE 企业版 | 1500000 点 | ¥799.2 |

**V0.2.0 推荐起手**：FREE 免费版（3000 资源点/月）。邮箱+密码登录 0 短信费，比短信方案节省一大笔。"""

text = text.replace(old_123, new_123)


# ============================================================
# Step 11: 整体重写 §12.4 准备清单
# ============================================================
old_124 = """### 12.4 腾讯云账号准备清单

1. **实名认证**：个人认证 / 企业认证（推荐企业，后续可商用）
2. **开通短信服务**：腾讯云控制台 → 短信 → 0 元下单首开通奖
3. **申请短信签名**：「流 Flow」（1-2 工作日审核）
4. **申请短信模板**：「验证码模板」（1-2 工作日审核）
5. **开通 CloudBase**：控制台 → 云开发 → 新建环境（推荐上海地域）
6. **创建数据库**：PostgreSQL 模式
7. **不部署云函数**：V0.2.0 采用 CloudBase 内置 Auth + RLS 直连数据库，0 后端代码"""

new_124 = """### 12.4 腾讯云账号准备清单

1. **实名认证**：个人认证 / 企业认证（推荐企业，后续可商用）
2. **开通 CloudBase**：控制台 → 云开发 → 新建环境（**地域任意**，不再受上海限制）
3. **创建数据库**：PostgreSQL 模式
4. **启用邮箱密码登录**：控制台 → 身份认证 → 登录方式 → 邮箱 + 用户名密码
5. **配置图像验证码风控**：控制台 → 身份认证 → 安全配置 → 图形验证码（默认配置即可）
6. **不部署云函数**：V0.2.0 采用 CloudBase 内置 Auth + RLS 直连数据库，0 后端代码

**总耗时对比短信方案**：
- 短信方案：1-3 天（实名 + 短信签名 + 模板审核）
- 邮箱密码方案：**30 分钟**（实名 + CloudBase 开通 + 控制台勾选）"""

text = text.replace(old_124, new_124)


# ============================================================
# Step 12: 删除 §13.4.5 sms_codes 表 + 调整 §13.6 云函数表
# ============================================================
old_1345 = """#### 13.4.5 sms_codes（验证码表，Redis 存储更佳）

```sql
-- 推荐使用 Redis，TTL = 5 分钟
-- 如必须用 PostgreSQL：
CREATE TABLE sms_codes (
  phone VARCHAR(20) PRIMARY KEY,
  code VARCHAR(10) NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  attempts INT DEFAULT 0,
  expires_at TIMESTAMPTZ NOT NULL
);
CREATE INDEX idx_sms_codes_expires ON sms_codes(expires_at);
```

"""

text = text.replace(old_1345, "")


# ============================================================
# Step 13: 调整 §13.6 表 - 图像验证码行 + 删 sendCode/verifyLogin
# ============================================================
old_136_table = """| 业务能力 | 原云函数方案 | V0.2.0 实际方案 |
|---------|------------|---------------|
| 发送验证码 | `sendCode` | CloudBase Auth 内置：客户端调 `POST /auth/v1/verification` |
| 验证码登录 | `verifyLogin` | CloudBase Auth 内置：客户端调 `POST /auth/v1/signin` |
| 写入 Session | `syncSession` | 客户端直调 `POST /v1/rdb/rest/sessions`，RLS 自动校验 owner_id |
| 读取所有数据 | `getUserData` | 客户端直调 `GET /v1/rdb/rest/sessions?select=*` |
| 写入 Settings | `syncSettings` | 客户端直调 `POST /v1/rdb/rest/settings` |
| 批量迁移 | `batchMigrate` | 客户端循环 `POST /v1/rdb/rest/sessions`，或一次性 bulk insert |"""

new_136_table = """| 业务能力 | 原云函数方案 | V0.2.0 实际方案 |
|---------|------------|---------------|
| 用户注册 | - | CloudBase Auth 内置：客户端调 `auth.signUp({email, password})` |
| 用户登录 | - | CloudBase Auth 内置：客户端调 `auth.signIn({email, password})` |
| 图像验证码 | - | CloudBase Auth 风控触发：客户端调 `auth.signIn({email, password, captcha_token})` |
| 写入 Session | `syncSession` | 客户端直调 `POST /v1/rdb/rest/sessions`，RLS 自动校验 owner_id |
| 读取所有数据 | `getUserData` | 客户端直调 `GET /v1/rdb/rest/sessions?select=*` |
| 写入 Settings | `syncSettings` | 客户端直调 `POST /v1/rdb/rest/settings` |
| 批量迁移 | `batchMigrate` | 客户端循环 `POST /v1/rdb/rest/sessions`，或一次性 bulk insert |"""

text = text.replace(old_136_table, new_136_table)


# ============================================================
# Step 14: §15.1 页面清单调整
# ============================================================
text = text.replace(
    "| 手机号输入 | `login.html`（主） | 新增 | 11 位手机号 |\n| 验证码输入 | `login.html`（同页切换） | 新增 | 6 位验证码 |",
    "| 登录页 | `login.html`（主） | 新增 | 邮箱 + 密码 |\n| 注册页 | `login.html`（切换） | 新增 | 邮箱 + 密码 + 确认 |\n| 图像验证码弹窗 | `login.html`（同页） | 新增 | 风控触发后显示 |"
)
text = text.replace(
    "- 「发送验证码」按钮使用 `primary-container` 浅色，与主屏形成视觉差",
    "- 「登录」按钮使用 `primary` 主题色；图像验证码弹窗用 `primary-container` 浅色"
)


# ============================================================
# Step 15: 整体重写 §15.3.1, 15.3.2, 15.3.3
# ============================================================
old_153 = """#### 15.3.1 手机号输入（login.html Step 1）

```
┌──────────────────────────────┐
│  ←                          │
│                              │
│                              │
│         [圆环 logo]           │  ← 主题色光晕
│                              │
│      欢迎回到 流 Flow         │  ← Headline 28px
│    登录后数据云端同步，多端互通 │  ← Body 14px 灰
│                              │
│  +86  ┌──────────────┐       │
│       │ 138 0000 0000 │       │  ← Outlined TextField
│       └──────────────┘       │
│                              │
│  ┌────────────────────────┐  │
│  │      获取验证码        │  │  ← Filled Button (primary)
│  └────────────────────────┘  │
│                              │
│                              │
│  登录即代表同意《服务协议》    │  ← Body 12px
│  《隐私政策》                │
└──────────────────────────────┘
```

**交互细节**：
- 手机号输入框自动 trim 空格
- 11 位长度自动校验
- 「获取验证码」按钮在手机号格式正确时高亮
- 点击 → loading 态（按钮内嵌 spinner）→ 成功后进入 Step 2
- 服务协议点击可弹出全屏模态

#### 15.3.2 验证码输入（login.html Step 2）

```
┌──────────────────────────────┐
│  ←                          │
│                              │
│      验证码已发送至           │
│      138 **** 1234           │  ← 脱敏显示
│                              │
│   ┌──┐┌──┐┌──┐┌──┐┌──┐┌──┐  │
│   │  ││ 1││ 2││ 3││ 4││ 5│  │  ← 6 个独立方框
│   └──┘└──┘└──┘└──┘└──┘└──┘  │
│                              │
│      59s 后重新发送            │  ← 倒计时
│                              │
│  ┌────────────────────────┐  │
│  │       登录             │  │  ← Filled Button
│  └────────────────────────┘  │
└──────────────────────────────┘
```

**交互细节**：
- 6 个独立方框，自动 focus 当前
- 支持粘贴自动填充（识别 6 位连续数字）
- 单个方框输入后自动 focus 下一格
- 倒计时 60s（持久化到 localStorage，防止刷新绕过）
- 60s 后切换为「重新发送」
- 错误时方框抖动 + 红色边框

#### 15.3.3 服务协议（agreement.html，模态弹窗）"""

new_153 = """#### 15.3.1 登录页（login.html 主视图）

```
┌──────────────────────────────┐
│  ←                          │
│                              │
│         [圆环 logo]           │  ← 主题色光晕
│                              │
│      欢迎回到 流 Flow         │  ← Headline 28px
│    登录后数据云端同步，多端互通 │  ← Body 14px 灰
│                              │
│  ┌────────────────────────┐  │
│  │ 邮箱                   │  │  ← Outlined TextField
│  │ your@email.com         │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │ 密码              👁    │  │  ← Outlined TextField
│  │ ••••••••               │  │  ← 密码可见切换
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │       登录             │  │  ← Filled Button (primary)
│  └────────────────────────┘  │
│                              │
│     没有账号？立即注册         │  ← Text Button
│                              │
│  登录即代表同意《服务协议》    │  ← Body 12px
│  《隐私政策》                │
└──────────────────────────────┘
```

**交互细节**：
- 邮箱输入框自动 trim 空格、自动 RFC 5322 格式校验
- 密码输入框支持显示/隐藏切换（眼睛图标）
- 「登录」按钮在邮箱+密码均非空时高亮
- 点击 → loading 态 → 成功后进入主屏
- 密码错误 3-5 次后弹图像验证码（见 15.3.3）

#### 15.3.2 注册页（login.html 切换态）

```
┌──────────────────────────────┐
│  ←                          │
│                              │
│      创建你的 流 Flow 账号     │  ← Headline 24px
│                              │
│  ┌────────────────────────┐  │
│  │ 邮箱                   │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │ 密码        8-32 位字母+数字│  │  ← Helper text 强度提示
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │ 确认密码                │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │       注册             │  │  ← 注册成功即自动登录
│  └────────────────────────┘  │
│                              │
│     已有账号？立即登录         │
└──────────────────────────────┘
```

**交互细节**：
- 邮箱重复检测：注册时若邮箱已存在，提示「该邮箱已注册，请直接登录」并跳回登录页
- 密码强度实时提示：弱 / 中 / 强（颜色从红到绿）
- 两次密码输入不一致：抖动 + 错误提示
- 注册成功后自动登录，无需再走登录流程

#### 15.3.3 图像验证码弹窗（风控触发）

```
┌──────────────────────────────┐
│  ╳                          │
│  请完成安全验证                │  ← Headline 18px
│                              │
│  ┌──────────┐               │
│  │ [验证码图片]│               │  ← 4 字符图片
│  │   a8K2   │               │  ← 来自 CloudBase HTTP API
│  └──────────┘                │
│      换一张  ↻                │  ← 刷新
│                              │
│  ┌────────────────────────┐  │
│  │ 请输入图片中的字符      │  │
│  └────────────────────────┘  │
│                              │
│  ┌────────────────────────┐  │
│  │       验证             │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

**触发条件**：
- 同一邮箱密码错误 3-5 次后自动弹出
- 客户端捕获 `CAPTCHA_REQUIRED` 错误码
- 调用 `GET /auth/v1/captcha` 拿图片 URL
- 渲染图片，用户输入字符
- 调用 `POST /auth/v1/captcha/verify` 拿 `captcha_token`
- 附带 `captcha_token` 重新发起登录

**设计原则**：
- 弹窗半透明遮罩 + 居中卡片，圆角 20px
- 图片背景随机干扰线，4 位大小写字母+数字
- 错误时抖动 + 提示「验证失败，请重试」
- 验证成功后自动关闭弹窗，继续登录

#### 15.3.4 服务协议（agreement.html，模态弹窗）"""

text = text.replace(old_153, new_153)


# ============================================================
# Step 16: §15.4 文案规范
# ============================================================
old_154 = """### 15.4 文案规范

| 场景 | 文案 |
|------|------|
| 主标题 | 欢迎回到 流 Flow |
| 副标题 | 登录后数据云端同步，多端互通 |
| 手机号 placeholder | 请输入手机号 |
| 验证码 placeholder | 请输入验证码 |
| 验证码副标题 | 验证码已发送至 138 **** 1234 |
| 倒计时 | 59s 后重新发送 |
| 重发按钮 | 重新发送 |
| 主按钮 | 获取验证码 / 登录 |
| 错误提示 | 验证码错误，还有 N 次机会 |
| 协议文案 | 登录即代表同意《服务协议》《隐私政策》 |"""

new_154 = """### 15.4 文案规范

| 场景 | 文案 |
|------|------|
| 登录主标题 | 欢迎回到 流 Flow |
| 登录副标题 | 登录后数据云端同步，多端互通 |
| 登录邮箱 placeholder | 请输入邮箱 |
| 登录密码 placeholder | 请输入密码 |
| 注册主标题 | 创建你的 流 Flow 账号 |
| 注册邮箱 placeholder | 请输入邮箱 |
| 注册密码 placeholder | 8-32 位字母 + 数字 |
| 注册确认密码 placeholder | 请再次输入密码 |
| 倒计时（注册） | 59s 后重新发送验证码（邮箱注册无需验证码） |
| 主按钮 | 登录 / 注册 |
| 错误提示 | 密码错误，还有 N 次机会 / 请输入正确的邮箱 |
| 协议文案 | 登录即代表同意《服务协议》《隐私政策》 |
| 图像验证码弹窗 | 请完成安全验证 / 请输入图片中的字符 |"""

text = text.replace(old_154, new_154)


# ============================================================
# Step 17: §16.1 安全
# ============================================================
old_161 = """### 16.1 安全

| 项 | 规范 |
|----|------|
| **验证码** | 6 位数字、5 分钟有效、单次使用 |
| **限流** | 1 分钟 1 次 / 1 小时 5 次 / 1 天 10 次（同一手机号） |
| **密码** | 本版本不存密码，验证码即密码 |
| **通信** | 全程 HTTPS / WSS |
| **Token** | JWT 2 小时有效期，自动续期 |
| **手机号展示** | 列表中脱敏 `138****1234` |
| **Token 存储** | Android EncryptedSharedPreferences / iOS Keychain |
| **登出** | 主动调用 CloudBase 失效接口 + 本地清除 |
| **防重放** | JWT 携带 jti，单次使用后失效 |
| **短信轰炸防护** | 腾讯云 SMS 自带防轰炸 + 自定义限流 |"""

new_161 = """### 16.1 安全

| 项 | 规范 |
|----|------|
| **密码** | 8-32 位字母+数字，CloudBase 自动 bcrypt 哈希存储 |
| **密码传输** | 全程 HTTPS，前端不存储明文 |
| **密码错误** | 连续 3-5 次触发图像验证码，1 天 10 次后临时锁定 1 小时 |
| **通信** | 全程 HTTPS / WSS |
| **Token** | JWT 2 小时有效期 + Refresh Token 30 天，SDK 自动续期 |
| **邮箱展示** | 列表中脱敏 `yo****@example.com` |
| **Token 存储** | Android EncryptedSharedPreferences / iOS Keychain |
| **登出** | 主动调用 `auth.signOut()` + 本地清除 |
| **图像验证码** | CloudBase 风控触发，4 位字符图片，HTTP API 校验 |
| **注册防刷** | 同邮箱 1 分钟 5 次，1 小时 20 次（同 IP） |"""

text = text.replace(old_161, new_161)


# ============================================================
# Step 18: §16.2 性能
# ============================================================
text = text.replace(
    "| 发送验证码端到端 | < 3s（含短信下发） |\n| 验证码校验端到端 | < 1.5s |",
    "| 邮箱密码登录端到端 | < 1.5s |\n| 图像验证码图片获取 | < 1s |\n| 图像验证码校验 + 重新登录 | < 2s |"
)


# ============================================================
# Step 19: §16.3 隐私
# ============================================================
text = text.replace(
    "| **强制登录** | 手机号是必填项（V0.2.0 政策决定） |",
    "| **强制登录** | 邮箱是必填项（V0.2.0 政策决定） |"
)
text = text.replace(
    "| **第三方 SDK** | 仅腾讯云 CloudBase + 腾讯云 SMS，无其他第三方 |",
    "| **第三方 SDK** | 仅腾讯云 CloudBase（含内置图像验证码 HTTP API），无其他第三方 |"
)


# ============================================================
# Step 20: §16.4 可用性
# ============================================================
old_164 = """### 16.4 可用性

| 场景 | 处理 |
|------|------|
| 网络异常 | 登录失败友好提示，按钮 loading 态恢复 |
| 短信到达失败 | 提供「语音验证码」备用通道（V0.2.0 后期） |
| 验证码错误 | 抖动反馈 + 倒计时未结束可重发 |
| Token 失效 | 自动跳登录页、保留本地数据 |
| 海外手机号 | 暂不支持（仅中国大陆 11 位手机号） |
| 老年用户 | 大字号模式（V0.3.0 规划） |"""

new_164 = """### 16.4 可用性

| 场景 | 处理 |
|------|------|
| 网络异常 | 登录失败友好提示，按钮 loading 态恢复 |
| 密码错误 | 抖动反馈 + 错误提示「密码错误，还有 N 次机会」 |
| 忘记密码 | V0.2.0 暂不支持邮箱链接重置（V0.3.0 规划），可联系客服 |
| 邮箱已注册 | 提示「该邮箱已注册，请直接登录」并自动跳转 |
| Token 失效 | 自动跳登录页、保留本地数据 |
| 国际邮箱 | 全支持（Gmail / Outlook / Yahoo 等） |
| 老年用户 | 大字号模式（V0.3.0 规划） |"""

text = text.replace(old_164, new_164)


# ============================================================
# Step 21: §16.5 合规
# ============================================================
text = text.replace(
    "- **隐私政策**：必须包含手机号收集说明、数据使用范围、用户权利",
    "- **隐私政策**：必须包含邮箱收集说明、数据使用范围、用户权利"
)
text = text.replace(
    "- **短信签名**：必须含 App 名称或公司名",
    "- **服务协议 + 隐私政策**：明确说明邮箱作为账号使用"
)


# ============================================================
# Step 22: §17 范围之外
# ============================================================
old_17 = """## 17. 范围之外（V0.2.0）

以下功能 V0.2.0 不做，明确边界：

- ❌ 邮箱注册 / 第三方登录（微信、QQ、Apple ID）
- ❌ 找回密码（验证码即密码，无需找回）
- ❌ 注销账户（V0.3.0 规划）
- ❌ 多设备同时在线冲突的复杂合并
- ❌ 商业化（会员、Pro 等级）
- ❌ 语音验证码（V0.2.0 后期评估）
- ❌ 国际手机号（仅中国大陆）
- ❌ 大字号 / 老年模式
- ❌ 头像上传（V0.2.0 暂不开放）
- ❌ 用户昵称（V0.2.0 用手机号脱敏展示）"""

new_17 = """## 17. 范围之外（V0.2.0）

以下功能 V0.2.0 不做，明确边界：

- ❌ 第三方登录（微信扫码、QQ、Apple ID）—— V0.3.0 评估
- ❌ 找回密码（V0.2.0 不支持邮箱链接重置）—— V0.3.0 规划
- ❌ 注销账户 —— V0.3.0 规划
- ❌ 多设备同时在线冲突的复杂合并
- ❌ 商业化（会员、Pro 等级）
- ❌ 每次登录都强制图像验证码（仅风控触发）
- ❌ 自定义图像验证码服务（用 CloudBase 内置）
- ❌ 大字号 / 老年模式
- ❌ 头像上传（V0.2.0 暂不开放）
- ❌ 用户昵称（V0.2.0 用邮箱脱敏展示）"""

text = text.replace(old_17, new_17)


# ============================================================
# Step 23: §18 验收
# ============================================================
text = text.replace(
    "- [ ] **login.html**（手机号 + 验证码 + 协议）",
    "- [ ] **login.html**（邮箱 + 密码 + 协议）"
)
text = text.replace(
    "- [ ] 「我的」页顶部显示当前登录手机号（脱敏）",
    "- [ ] 「我的」页顶部显示当前登录邮箱（脱敏）"
)
text = text.replace(
    "- [ ] 短信登录方式已开启（控制台 → 身份认证 → 登录方式）",
    "- [ ] 邮箱 + 用户名密码登录方式已开启（控制台 → 身份认证 → 登录方式）"
)
text = text.replace(
    "- [ ] 抽象函数 `auth.uid()` / `auth.role()` 已创建\n- [ ] RLS 启用 + 表级 GRANT + 4 套 Policy 每表齐备",
    "- [ ] 抽象函数 `auth.uid()` / `auth.role()` 已创建\n- [ ] RLS 启用 + 表级 GRANT + 4 套 Policy 每表齐备\n- [ ] 图像验证码风控测试：密码错 5 次后必触发"
)
text = text.replace(
    "- [ ] 手机号 / 验证码输入流畅，无明显卡顿",
    "- [ ] 邮箱 / 密码输入流畅，密码可见切换工作正常"
)


# ============================================================
# Step 24: §19 风险 + 依赖
# ============================================================
old_19 = """### 19.1 风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| 短信到达率不稳定 | 用户登录失败 | 「语音验证码」备用通道（V0.2.0 后期）|
| 用户抗拒强制登录 | 老用户流失 | 引导文案 + 服务协议透明化 + 数据迁移说明 |
| 短信成本失控 | 财务风险 | 限流 + 监控 + 异常告警 + 每月预算上限 |
| CloudBase 资源点耗尽 | 服务不可用 | 设置 80% 阈值告警、自动降级为本地模式 |
| 实名认证审核拖延 | 发布延期 | 提前 1 周开始准备材料 |
| 短信签名 / 模板审核失败 | 登录功能无法上线 | 提前 1 周申请，备选 2-3 个签名方案 |
| RLS 策略漏洞 | 数据泄露 | 安全测试 + 渗透测试 + 第三方审计 |
| 并发同步冲突 | 数据错乱 | lastModifiedAt 时间戳 + 用户级锁 |

### 19.2 依赖

| 依赖 | 状态 | 备注 |
|------|------|------|
| 腾讯云账号实名认证 | 待办 | 个人认证 1 天 / 企业认证 3-7 天 |
| 短信签名「流 Flow」审核 | 待办 | 1-2 工作日 |
| 短信模板审核 | 待办 | 1-2 工作日 |
| CloudBase 环境开通 | 待办 | 实名后即时 |
| PostgreSQL 数据库创建 | 待办 | CloudBase 控制台一键 |
| 5 张表结构创建 | 待办 | SQL 脚本一次性执行 |
| 6 个云函数部署 | 待办 | 详见 §13.6 |
| App 备案（如上架） | 待办 | ICP 备案 7-20 天 |"""

new_19 = """### 19.1 风险

| 风险 | 影响 | 缓解 |
|------|------|------|
| 密码撞库攻击 | 账户被盗 | 强制 8-32 位密码 + 图像验证码风控 + bcrypt 哈希 |
| 用户抗拒强制登录 | 老用户流失 | 引导文案 + 服务协议透明化 + 数据迁移说明 |
| CloudBase 资源点耗尽 | 服务不可用 | 设置 80% 阈值告警、自动降级为本地模式 |
| 实名认证审核拖延 | 发布延期 | 提前 1 周开始准备材料 |
| RLS 策略漏洞 | 数据泄露 | 安全测试 + 渗透测试 + 第三方审计 |
| 并发同步冲突 | 数据错乱 | lastModifiedAt 时间戳 + 用户级锁 |
| 图像验证码被 OCR 绕过 | 风险降低 | CloudBase 风控会动态升级（滑块 / 拼图） |

### 19.2 依赖

| 依赖 | 状态 | 备注 |
|------|------|------|
| 腾讯云账号实名认证 | 待办 | 个人认证 1 天 / 企业认证 3-7 天 |
| CloudBase 环境开通 | 待办 | 实名后即时，**地域不再受限**（邮箱密码全地域支持） |
| PostgreSQL 数据库创建 | 待办 | CloudBase 控制台一键 |
| 3 张表结构创建 | 待办 | SQL 脚本一次性执行 |
| 邮箱密码登录方式开启 | 待办 | 控制台勾选，5 分钟完成 |
| 图像验证码风控配置 | 待办 | 控制台默认配置，无需操作 |
| 0 个云函数部署 | - | V0.2.0 不部署 |
| App 备案（如上架） | 待办 | ICP 备案 7-20 天 |"""

text = text.replace(old_19, new_19)


# ============================================================
# Step 25: §19.3 数据来源
# ============================================================
text = text.replace(
    "- 腾讯云 SMS 价格：https://buy.cloud.tencent.com/pricing/sms\n- 腾讯云 SMS 国内短信计费概述：https://cloud.tencent.com/document/product/248/36132\n- 腾讯云 SMS 价格调整公告（2026-04-01）：https://cloud.tencent.com.cn/announce/detail/2222\n- CloudBase 产品介绍：https://docs.cloudbase.net/\n- CloudBase 定价：https://www.cloudbase.net/pricing\n- CloudBase Supabase 版：https://cloud.tencent.com/product/tcbs",
    "- CloudBase 邮箱密码登录：https://docs.cloudbase.net/authentication/method/username-login\n- CloudBase 登录认证 v2：https://docs.cloudbase.net/en/api-reference/webv2/authentication_v2\n- CloudBase 身份认证概述：https://cloud.tencent.com/document/product/876/121347\n- CloudBase 图形验证码处理：https://docs.cloudbase.net/service/authentication\n- CloudBase 产品介绍：https://docs.cloudbase.net/\n- CloudBase 定价：https://www.cloudbase.net/pricing"
)


# ============================================================
# Step 26: §20 决策记录 - 关键决策调整
# ============================================================
text = text.replace(
    "| 认证方式 | 微信扫码 / 手机号+短信 / 双通道 / 邮箱密码 | **手机号+短信** | 最通用 + 腾讯云 SMS 价格低 + 验证码即密码 |",
    "| 认证方式 | 微信扫码 / 手机号+短信 / 邮箱+密码 | **邮箱+密码+图像验证码** | CloudBase Auth v2 原生最简，0 短信费，0 后端代码 |"
)


# ============================================================
# Step 27: §21 下一步计划
# ============================================================
text = text.replace(
    "| P0 | 腾讯云账号实名 + 短信签名模板申请 | 3-5 天 | 无 |",
    "| P0 | 腾讯云账号实名 + CloudBase 环境开通 | 1-2 天 | 无 |"
)


# ============================================================
# 写入
# ============================================================
PATH.write_text(text, encoding="utf-8")
print(f"[ok] {PATH}")
print(f"     {len(text):,} chars, {len(text) / 1024:.1f} KB")
